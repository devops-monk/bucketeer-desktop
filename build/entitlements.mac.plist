<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Electron needs these under the hardened runtime; V8 compiles JIT at runtime. -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Required while builds are ad-hoc signed: library validation would otherwise
         reject Electron's own frameworks, since they carry a different signature.
         Can be removed once builds are signed with a real Developer ID. -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
    <!-- Talking to S3 and STS. -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Reading files the user picks for upload, and writing downloads. -->
    <key>com.apple.security.files.user-selected.read-write</key>
    <true/>
  </dict>
</plist>
